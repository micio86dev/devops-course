# unit + integration + api
pytest
pytest-cov
pytest-flask

# e2e
pytest-playwright

# linting + type checking
ruff
mypy
types-PyMySQL  # used by scripts/sqlite_to_mysql.py

# After installing, run: playwright install chromium
